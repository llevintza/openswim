# Design tokens

Shared visual tokens for web and native clients (colors, spacing, type scale notes).

Keep platform implementations native; this package documents the shared vocabulary only.

## Status

Scaffold only.
