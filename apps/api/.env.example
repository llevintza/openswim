# OpenSwim API — local env example
# Copy to .env (never commit secrets). Prefer running inside the Dev Container.

PORT=8080
DATABASE_URL=postgres://openswim:openswim@postgres:5432/openswim?sslmode=disable
JWT_SECRET=dev-only-change-me
