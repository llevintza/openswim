# OpenSwim Web

Next.js (App Router) TypeScript client for clubhouse admin, fan meet views, and chat.

## Intended stack

- Next.js App Router
- TypeScript
- Talks to `apps/api` over REST + WebSocket

## Status

Scaffold only — app not bootstrapped yet.
