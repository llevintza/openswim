# GitHub

CI workflow files belong in `workflows/` once a credential with the `workflow` scope can push them.

Until then, keep this directory for Copilot instructions and other non-workflow GitHub metadata.

